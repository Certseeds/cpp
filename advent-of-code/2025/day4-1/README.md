# day4-1

第一眼感觉这个是个图问题, 但是仔细看规则是固定的, 首先 `@` 标志代表1, `.` 代表0, 然后只需要判断 `@` 标志周围有多少 `@`标志, if less then 4, 则可以.

1. 先读取所有输入, 然后按照行数和列数新建二维数组(注意prefix, postfix各自新增全零来填充)
2. 对二维数组求一个卷积, kernel = [[1,1,1],[1,0,1],[1,1,1]], 然后遍历原始数组, if @ then return convolution结果 < 4 else False

